# Learning Log — 2026-03-17

---

#### BYOK implementation added to backend
- Added `anthropic_api_key` field to User model
- New Alembic migration: `18a03e8cf3e1_add_anthropic_api_key_to_users.py`
- Executor uses user's own API key if set, falls back to platform key
- Settings endpoint added for users to save/update their key

#### Port conflict resolution
- Other projects may use port 3000/8000 on the same machine
- Solution: run DalkkakAI frontend on port 3001
- Docker containers are isolated — no conflict with host services on same port
- Updated CORS_ORIGINS to allow both localhost:3000 and localhost:3001

#### Session detail panel UI built
- Clickable session cards open right-side detail panel
- Shows conversation history, files changed, cost, chat input
- Session type filter: Active / Ready to Review / History sections
- Chat input calls POST /sessions/{id}/chat

#### Docker image must be rebuilt after Dockerfile changes
- `docker-compose up -d` reuses old image
- `docker-compose up -d --build` forces rebuild
- Critical when adding system packages (like git)
